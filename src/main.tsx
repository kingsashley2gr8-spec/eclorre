import React from 'react'
import ReactDOM from 'react-dom/client'
import EcloreLanding from './app'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <EcloreLanding />
  </React.StrictMode>
)
